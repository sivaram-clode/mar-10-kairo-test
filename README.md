# Notes App

A minimal notes-taking app built with Vite + React. Notes are stored in `localStorage`.

## Features

- Create notes with a title and body
- View note details by clicking a note card
- Delete notes
- Data persists across page reloads via localStorage

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:5173
